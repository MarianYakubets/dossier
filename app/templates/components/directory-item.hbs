<div class="panel panel-default directory-item">
    <div class="panel-heading">
        <h3 class="panel-title">{{item.name}}</h3>
    </div>
    <div class="panel-body">
        <p>Address: {{item.name}}</p>
        <p>Phone: {{item.type}}</p>
    </div>
    <div class="panel-footer text-right">
      {{yield}}
    </div>
</div>
